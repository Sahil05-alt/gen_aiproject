services:
  - type: web
    name: docmind-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    autoDeployTrigger: commit
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: GROQ_MODEL
        value: llama-3.3-70b-versatile
      - key: EMBEDDING_MODEL
        value: sentence-transformers/all-MiniLM-L6-v2
      - key: CHUNK_SIZE
        value: "512"
      - key: CHUNK_OVERLAP
        value: "50"
      - key: TOP_K
        value: "5"
      - key: VECTOR_STORE_PATH
        value: /var/data/docmind/vectorstore
      - key: UPLOADS_PATH
        value: /var/data/docmind/uploads
      - key: METADATA_PATH
        value: /var/data/docmind/metadata
      - key: CORS_ORIGINS
        sync: false
    disk:
      name: docmind-data
      mountPath: /var/data/docmind
      sizeGB: 10
